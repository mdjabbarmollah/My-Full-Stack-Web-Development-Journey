// je gulo na janle function ki setai bojha jabe na
// how to decleare a function

// function firstfunction() { 
// }

// function divide(num1, num2) {
//   const result = num1 / num2;
//   console.log("divide result", result);
// }
// how to call a function 
// firstfunction() //must jante hobe

// how to deal with parameter
// divide(500, 8);

// how to deal with return 
function addAll(a,b,c,d) {
  const firstsum = a + b;
  const seconSum = c + b;
  const multiply = firstsum * seconSum;
  const divided = multiply / 4;
  return divided;
 
}
// chat gpt thke jevabe bujhlam tate call with variable hobe
const result = addAll(a,b,c,d);

const result = addAll(4, 8, 16, 24);
const fahad = 2;

// call function with variable

divided(result);//eta bujhi nai

// // ১. প্রথমে কিছু ভ্যারিয়েবলে মানগুলো রেখে দিলাম
// const num1 = 4;
// const num2 = 8;
// const num3 = 16;
// const num4 = 24;

// // ২. এখন ফাংশন কল করার সময় সরাসরি ওই ভ্যারিয়েবলগুলোর নাম (আর্গুমেন্ট হিসেবে) পাস করে দিলাম
// const result = addAll(num1, num2, num3, num4);

// console.log(result);
 